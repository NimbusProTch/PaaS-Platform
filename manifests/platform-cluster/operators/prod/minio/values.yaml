# MinIO Production Configuration
clusterName: demo-minio
namespace: demo-prod
tenant: demo
environment: prod

replicas: 2
volumesPerServer: 2

storage:
  size: "10Gi"
  storageClass: "standard"

resources:
  requests:
    cpu: "250m"
    memory: "512Mi"
  limits:
    cpu: "500m"
    memory: "1Gi"
