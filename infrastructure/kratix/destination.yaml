# Platform Destination
apiVersion: platform.kratix.io/v1alpha1
kind: Destination
metadata:
  name: platform-cluster
  labels:
    infraforge.io/platform: "true"
spec:
  path: .
  filepath:
    mode: none  # Flat structure - pipeline controls paths
  stateStoreRef:
    name: github-store
    kind: GitStateStore