---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bootstrapclaims.platform.infraforge.io
spec:
  group: platform.infraforge.io
  names:
    kind: BootstrapClaim
    listKind: BootstrapClaimList
    plural: bootstrapclaims
    singular: bootstrapclaim
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: BootstrapClaim is the Schema for the bootstrapclaims API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BootstrapClaimSpec defines the desired state of BootstrapClaim
            properties:
              gitOps:
                description: GitOps configuration
                properties:
                  branch:
                    description: 'Branch name for GitOps (default: "main")'
                    type: string
                  clusterType:
                    description: ClusterType for root app generation (nonprod/prod)
                    type: string
                  environments:
                    description: 'Environments to create (default: ["dev", "qa", "sandbox",
                      "staging", "prod"])'
                    items:
                      type: string
                    type: array
                type: object
              giteaURL:
                description: GiteaURL is the URL of the Gitea server
                type: string
              organization:
                description: Organization is the Gitea organization name
                type: string
              repositories:
                description: Repositories to create and initialize
                properties:
                  charts:
                    description: 'Charts repository name - contains both microservice
                      and platform templates (default: "charts")'
                    type: string
                  voltran:
                    description: 'Voltran GitOps config repository name (default:
                      "voltran")'
                    type: string
                type: object
            required:
            - gitOps
            - giteaURL
            - organization
            - repositories
            type: object
          status:
            description: BootstrapClaimStatus defines the observed state of BootstrapClaim
            properties:
              chartsUploaded:
                description: ChartsUploaded tracks chart upload status
                type: boolean
              conditions:
                description: Conditions detailed conditions
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastUpdated:
                description: LastUpdated last update timestamp
                format: date-time
                type: string
              message:
                description: Message provides additional status information
                type: string
              phase:
                description: Phase current phase (Pending, Bootstrapping, Ready, Failed)
                type: string
              ready:
                description: Ready overall readiness status
                type: boolean
              repositoriesCreated:
                description: RepositoriesCreated tracks repository creation
                type: boolean
              repositoryURLs:
                additionalProperties:
                  type: string
                description: RepositoryURLs created repository URLs
                type: object
              rootAppGenerated:
                description: RootAppGenerated tracks root app generation
                type: boolean
            required:
            - chartsUploaded
            - ready
            - repositoriesCreated
            - rootAppGenerated
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
