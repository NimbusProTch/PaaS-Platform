---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: platformclaims.platform.infraforge.io
spec:
  group: platform.infraforge.io
  names:
    kind: PlatformClaim
    listKind: PlatformClaimList
    plural: platformclaims
    singular: platformclaim
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.environment
      name: Environment
      type: string
    - jsonPath: .spec.clusterType
      name: ClusterType
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: PlatformClaim is the Schema for the platformclaims API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PlatformClaimSpec defines the desired state of PlatformClaim
            properties:
              clusterType:
                description: ClusterType cluster type (nonprod, prod)
                type: string
              environment:
                description: Environment deployment environment (dev, qa, sandbox,
                  staging, prod)
                type: string
              namespace:
                description: Namespace target namespace (auto-generated if empty)
                type: string
              owner:
                description: Owner team ownership information
                properties:
                  email:
                    description: Email contact email
                    type: string
                  slack:
                    description: Slack slack channel
                    type: string
                  team:
                    description: Team team name
                    type: string
                required:
                - email
                - team
                type: object
              services:
                description: Services platform services to deploy
                items:
                  description: PlatformServiceSpec defines a platform service configuration
                  properties:
                    backup:
                      description: Backup enable backup configuration
                      properties:
                        enabled:
                          description: Enabled enable backups
                          type: boolean
                        retention:
                          description: Retention retention period in days
                          type: integer
                        schedule:
                          description: Schedule cron schedule for backups
                          type: string
                        storageClass:
                          description: StorageClass storage class for backup volumes
                          type: string
                      required:
                      - enabled
                      type: object
                    chart:
                      description: Chart Helm chart configuration
                      properties:
                        name:
                          description: Name chart name
                          type: string
                        repository:
                          description: Repository Helm repository URL (if external)
                          type: string
                        source:
                          description: Source chart source (embedded, bitnami, elastic,
                            etc.)
                          type: string
                        version:
                          description: Version chart version
                          type: string
                      required:
                      - name
                      type: object
                    highAvailability:
                      description: HighAvailability enable HA configuration
                      type: boolean
                    monitoring:
                      description: Monitoring enable monitoring
                      type: boolean
                    name:
                      description: Name service name (e.g., "postgres", "redis", "rabbitmq")
                      type: string
                    size:
                      description: Size configuration size (small, medium, large)
                      type: string
                    type:
                      description: Type service type (postgresql, redis, rabbitmq,
                        mongodb, mysql, kafka, elasticsearch)
                      type: string
                    values:
                      description: Values custom values for the service
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    version:
                      description: Version service version (optional)
                      type: string
                  required:
                  - chart
                  - name
                  - type
                  type: object
                type: array
            required:
            - clusterType
            - environment
            - owner
            - services
            type: object
          status:
            description: PlatformClaimStatus defines the observed state of PlatformClaim
            properties:
              conditions:
                description: Conditions detailed conditions
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastUpdated:
                description: LastUpdated last update timestamp
                format: date-time
                type: string
              message:
                description: Message provides additional status information
                type: string
              phase:
                description: Phase current phase (Pending, Provisioning, Ready, Failed)
                type: string
              ready:
                description: Ready overall readiness status
                type: boolean
              services:
                description: Services service statuses
                items:
                  description: PlatformServiceStatus defines the status of a platform
                    service
                  properties:
                    endpoint:
                      description: Endpoint service endpoint
                      type: string
                    message:
                      description: Message additional status message
                      type: string
                    name:
                      description: Name service name
                      type: string
                    ready:
                      description: Ready service ready status
                      type: boolean
                    secretName:
                      description: SecretName secret containing credentials
                      type: string
                    type:
                      description: Type service type
                      type: string
                    version:
                      description: Version deployed version
                      type: string
                  required:
                  - name
                  - ready
                  - type
                  type: object
                type: array
              servicesReady:
                description: ServicesReady all services ready
                type: boolean
            required:
            - ready
            - servicesReady
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
