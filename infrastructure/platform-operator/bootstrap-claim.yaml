apiVersion: platform.infraforge.io/v1
kind: BootstrapClaim
metadata:
  name: platform-bootstrap
spec:
  giteaURL: http://gitea-http.gitea.svc.cluster.local:3000
  organization: infraforge

  # OCI Charts Repository - GitHub Packages
  chartsRepository:
    type: oci
    url: oci://ghcr.io/infraforge  # Base URL, her chart ayrı package
    version: "1.0.0"

  repositories:
    voltran: voltran

  gitOps:
    branch: main
    clusterType: nonprod
    environments:
      - dev
      - qa
      - staging
