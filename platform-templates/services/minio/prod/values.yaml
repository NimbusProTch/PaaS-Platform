# MinIO Production Configuration
clusterName: minio
namespace: default
tenant: demo
environment: prod

replicas: 4
volumesPerServer: 4

storage:
  size: "50Gi"
  storageClass: "standard"

resources:
  requests:
    cpu: "1000m"
    memory: "2Gi"
  limits:
    cpu: "2000m"
    memory: "4Gi"
